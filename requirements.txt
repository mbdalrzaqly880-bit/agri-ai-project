# ملف requirements.txt
# فيه كل المكتبات اللي يحتاجها المشروع عشان يشتغل
# لتثبيتهم كلهم دفعة وحدة، تكتب بالـ Terminal: pip install -r requirements.txt
# هذا مفيد جداً لو غيّرت جهاز أو رفعت المشروع لسيرفر - ما تحتاج تثبت كل مكتبة يدوياً

Flask==3.0.3
Flask-SQLAlchemy==3.1.1

# requests: نستخدمها لإرسال طلبات HTTP لـ Gemini API
requests==2.32.3

# python-dotenv: تقرأ ملف .env وتحمّل المفاتيح السرية كمتغيرات بيئة
python-dotenv==1.0.1

# gunicorn: سيرفر إنتاجي حقيقي (Production Server) يستخدمه Render لتشغيل
# الموقع - سيرفر Flask المدمج (app.run) مخصص للتطوير المحلي فقط، مو للنشر
gunicorn==23.0.0


# ==============================================================================
# المكتبات التالية مطلوبة لتشغيل الموديل الحقيقي (model/model_utils.py)
# ==============================================================================
tensorflow==2.17.0
numpy==1.26.4
Pillow==10.4.0


